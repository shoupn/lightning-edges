
version: '3.8'
services:
  database:

    image: "postgres:latest"
    env_file:
      - .env # configure postgres
    ports:
      - "5432:5432"
    volumes:
      - pg-data:/var/lib/postgresql/data/ 
volumes:
  pg-data: 